const fs = require('fs'); let code = fs.readFileSync('js/main.js', 'utf8'); code = code.replace(/document\\.addEventListener\\('mousedown', function\\(e\\)\\{\\s*var el = _getZoomTarget\\(e\\.target\\);\\s*if\\(!el\\) return;\\s*if\\(e\\.button !== 0\\) return;/, window.spaceBarPressed = false;\nwindow.addEventListener('keydown', e => { if (e.code === 'Space' && document.activeElement.tagName !== 'INPUT' && document.activeElement.tagName !== 'TEXTAREA') { window.spaceBarPressed = true; if(document.activeElement.tagName !== 'INPUT' && document.activeElement.tagName !== 'TEXTAREA') e.preventDefault(); } });\nwindow.addEventListener('keyup', e => { if (e.code === 'Space') window.spaceBarPressed = false; });\n\ndocument.addEventListener('mousedown', function(e){\n    var el = _getZoomTarget(e.target);\n    if(!el) return;\n    if(e.button === 0 && !window.spaceBarPressed) return;\n    if(e.button !== 0 && e.button !== 1) return;); fs.writeFileSync('js/main.js', code);
