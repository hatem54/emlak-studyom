const fs = require('fs'); let content = fs.readFileSync('core/drag.js', 'latin1'); content = content.replace(/if \(!wasSelected\) \{\s*e\.preventDefault\(\);\s*\}/, 'if (!wasSelected) { e.preventDefault(); if (el.classList.contains(\'editable-draw\') && (e.pointerType === \'pen\' || e.pointerType === \'touch\' || e.touches)) { return; } }'); content = content.replace('if(el.dataset.editingText){dragging=false;resizing=false;return}', 'if(el.dataset.editingText){dragging=false;resizing=false;return}\n        if (e.pointerType === \'pen\' && e.buttons === 0) return;'); fs.writeFileSync('core/drag.js', content, 'latin1');
