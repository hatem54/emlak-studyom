const fs = require('fs'); let code = fs.readFileSync('modules/draw.js', 'utf8'); code = code.replace(/function setDrawMode\\(mode\\)\\{[\\s\\S]*?if \\(typeof deselectAll === 'function'\\) deselectAll\\(\\);\\s*\\}/, 'function setDrawMode(mode){\n    if (mode === \'off\') {\n        if (typeof deselectAll === \'function\') deselectAll();\n    } else {\n        document.querySelectorAll(\'.editable-draw\').forEach(el => el.style.pointerEvents = \'none\');\n        if (typeof deselectAll === \'function\') deselectAll();\n    }'); fs.writeFileSync('modules/draw.js', code);
